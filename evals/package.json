{
  "name": "rubion-skills-evals",
  "version": "1.0.0",
  "private": true,
  "description": "Skill description trigger accuracy evals",
  "scripts": {
    "eval": "tsx runner.ts --all",
    "eval:changed": "tsx runner.ts --changed-only",
    "eval:verbose": "tsx runner.ts --all --verbose"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.37.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
